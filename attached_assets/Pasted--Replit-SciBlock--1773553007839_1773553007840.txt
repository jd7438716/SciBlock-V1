我们现在的目标不是立刻把项目迁出 Replit，而是先把当前 SciBlock 项目整理成“可迁移”的标准工程，确保未来可以平滑部署到自己的云服务器。

请基于当前项目真实代码，执行一次“可迁移性改造梳理”，重点不是新功能，而是工程规范化、部署解耦、环境解耦、数据解耦。

请按下面要求输出，并尽量直接落到当前代码结构中，不要给泛泛建议。

一、目标
请围绕以下目标进行整理：
1. 项目未来可以脱离 Replit 独立部署
2. 不依赖 Replit 特有运行方式才能启动
3. 前端、Express 后端、Go 后端、数据库之间边界清晰
4. 所有持久化数据都在标准存储中，而不是浏览器 storage 或本地文件系统
5. 环境变量、构建方式、启动方式、数据库迁移都标准化
6. 后续迁移到云服务器时，只需要：
   - 拉代码
   - 配环境变量
   - 初始化数据库
   - 启动服务
   - 配反向代理
即可运行

二、请先审计当前项目中哪些地方“不利于迁移”
请基于当前代码，逐项列出所有不利于未来迁移部署的点，至少覆盖这些方面：
1. 哪些关键数据还存在 localStorage / sessionStorage
2. 哪些地方依赖 Replit 的开发/运行习惯
3. 哪些服务启动方式还不标准
4. 哪些环境变量还不完整或未收敛
5. 哪些数据库结构修改没有形成标准 migration
6. 哪些 API 路径、端口、代理方式未来迁移时会有问题
7. 哪些目录结构或脚本组织方式不利于独立部署
8. 哪些前端页面还强依赖 mock 数据
9. 哪些文件上传/附件逻辑未来会阻碍迁移
10. 哪些地方还存在 demo 式写法，不利于长期维护

三、请给出当前项目的“可迁移工程规范方案”
请输出一个基于当前项目的规范方案，至少包括：

1. 代码结构规范
请说明当前项目如何整理为适合未来独立部署的结构。
需要分别说明：
- 前端目录职责
- Express 后端目录职责
- Go 后端目录职责
- 共享库（lib）职责
- scripts 职责
- migration 文件放在哪里
- 哪些生成产物不应该作为核心依赖

2. 环境变量规范
请整理出一份完整的环境变量清单，并区分：
- 前端运行变量
- Express 后端变量
- Go 后端变量
- 数据库变量
- AI 接口变量
- 后续对象存储预留变量

请输出：
- 变量名
- 用途
- 是否必需
- 示例值
- 属于哪个服务

并说明哪些变量现在缺失，需要补齐。

3. 启动与构建规范
请整理出项目未来要具备的标准启动方式：
- 前端本地开发启动命令
- Express 本地开发启动命令
- Go 本地开发启动命令
- 前端生产构建命令
- Express 生产启动命令
- Go 生产启动命令

要求：
- 不依赖 Replit UI 操作才能启动
- 尽量通过 package.json / Makefile / 脚本文件统一入口
- 明确哪些命令当前已经存在，哪些需要新增

4. 数据持久化规范
请明确指出：
- 哪些数据必须尽快从 localStorage / sessionStorage 迁到数据库
- 哪些数据可以暂时保留本地 fallback
- 哪些数据以后必须进入对象存储而不是数据库
- 当前项目里哪些地方绝不能继续依赖浏览器 storage 作为正式数据源

5. 数据库迁移规范
请基于当前 PostgreSQL + Drizzle / 未来 Go 后端的情况，给出迁移规范：
- 以后所有建表/改表是否都必须通过 migration 文件
- 当前已有表结构变更如何记录
- Express 与 Go 共用数据库时如何避免表结构混乱
- migration 目录建议怎么组织
- 哪些现有表需要尽快补 FK / role / user-student 关联

四、请输出一份“迁移前必须完成的改造清单”
请按优先级输出一个 checklist，分成三层：

P0（必须尽快做，否则未来很难迁移）
P1（建议在 MVP 阶段完成）
P2（可以后续优化）

例如可包含但不限于：
- SciNote 落库
- ExperimentRecord 落库
- JWT 认证统一
- users.role 增加
- students.user_id 增加
- .env.example 补全
- 前后端启动命令标准化
- migration 标准化
- 附件改为对象存储接口预留
- 去掉关键 mock 依赖
- 统一 API base URL 配置
- 标准化 build 输出目录
- 预留 Nginx / 反向代理兼容路径

五、请生成项目所需的标准化文件建议
请列出当前项目为了“可迁移”应新增或补齐哪些工程文件，例如：
- 根目录 README.md
- deploy/README.md
- .env.example
- .env.server.example
- .env.web.example
- Makefile
- docker-compose.yml（如果你认为当前还不该做，请明确说明）
- scripts/dev.sh
- scripts/build.sh
- scripts/start.sh
- migration README
- API 服务启动说明
- 数据库初始化说明

请对每个文件说明：
- 是否建议现在就创建
- 它的作用
- 应放在哪个目录
- 内容应包含什么

六、请给出“未来迁移到云服务器”的兼容部署方案
注意：不是现在就部署，而是提前为未来做兼容。
请基于当前项目给出一个兼容未来迁移的部署拓扑建议，例如：
- Nginx
- 前端静态资源
- Express API
- Go API
- PostgreSQL
- 未来对象存储

请说明：
1. 哪些路径以后建议由 Nginx 转发到 Express
2. 哪些路径以后建议由 Nginx 转发到 Go
3. 前端打包产物未来应该如何独立部署
4. 现在代码里要避免哪些写法，才能兼容这种部署方式

七、请给出实际落地建议
不要只说原则，请明确回答：
1. 当前项目为了“可迁移”，最先该改哪 5 件事
2. 哪些事情现在不要做，避免过度工程化
3. 哪些内容只要保持规范，未来迁移时就会很顺
4. 请尽量引用当前真实文件名、目录名、context 名、storage key、API 模块名来说明

要求：
- 必须基于当前代码真实情况输出
- 不要给空泛模板
- 不要重新定义整个项目
- 不要脱离当前 Express + React + 准备新增 Go 的现实情况
- 输出要尽量具体，方便我直接照着推进改造