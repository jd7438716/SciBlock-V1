先暂停新增功能开发，当前优先级切换为：
代码结构治理 / 解耦 / 模块化优化
这次不是加新功能，而是对现有代码做一次系统性的结构整理。
目标是把现在已经做出来的能力，从“能跑”提升到“可长期维护、可持续扩展”。
一、目标
当前项目已经连续加入了多条主链路：
实验记录传承链
周报批阅 / 评论 / 消息链
周报 ↔ 实验记录联动
AI 周报生成
多日期选择模型
导师端只读查看链路（部分）
现在最重要的不是继续堆功能，而是做一次工程化收口，重点解决：
代码耦合过深
页面组件过重
业务逻辑散落在前端页面里
API 调用与业务语义混杂
类型定义分散
重复逻辑增多
后续继续开发会越来越难维护
二、这次任务不是功能开发，而是结构优化
请你先不要继续做新功能，先对当前代码结构做一次系统性审查，然后给我一份结构治理方案，再开始实施。
三、重点检查和治理的方向
1）前端页面层是否过重
请重点审查以下页面 / 面板 / context 是否已经承担了过多职责：
WorkbenchContext
ExperimentWorkbenchPage
GenerateReportWizard
ReportWorkPanel
TeamReportDetailPanel
成员详情相关页面
周报详情相关页面
请判断这些文件里是否存在：
状态过多
业务逻辑过多
同时做数据获取、数据转换、UI 渲染
一个文件同时承担页面编排 + 业务规则 + API 处理 + 展示细节
如果有，请提出拆分方案。
2）业务逻辑是否散落在页面和组件里
请重点排查这些业务逻辑是否被写散了：
实验记录传承链判断
confirmed / confirmed_dirty 状态流转
head / historical 语义判断
周报 links / selected_dates 逻辑
AI 周报生成状态与来源判断
删除权限判断
导师端只读语义判断
这些逻辑应该尽量收口到：
service 层
hook 层
selector / util 层
或明确的 domain 层
不要散在多个页面组件里各写一份。
3）前端组件是否应该分层
请你检查当前前端是否可以明确拆成几层：
页面编排层
业务组件层
纯展示组件层
hooks / view-model 层
API client 层
domain utils / selectors 层
types 层
如果当前还没有清晰分层，请你给出具体的重构建议。
4）API client / 类型 / DTO 是否足够统一
请检查：
实验记录相关类型是否集中管理
周报相关类型是否集中管理
links / selected_dates / AI generation metadata 是否类型定义统一
学生端 / 导师端是否有重复定义
API client 返回类型和页面实际使用类型是否混乱
如果有重复、分散、命名不统一的问题，请给出整理方案。
5）只读视图和编辑视图是否混杂
这个我特别在意。
现在系统里已经明显存在两套语义：
学生编辑态
导师只读态
请检查当前代码里是否有：
同一个组件既服务编辑态又服务只读态，导致 props 越来越复杂
学生端逻辑泄露到导师端
导师端只是“禁用按钮”而不是结构上真的只读
如果有，请提出结构化拆分方案。
6）重复逻辑和重复 UI 是否在增加
请排查是否存在下面这些重复项：
相同的状态 badge 逻辑重复写
links / dates / inheritance / dirty 提示在多个地方重复拼装
周报和实验记录的只读区块结构重复
同样的空状态 / 错误状态 / loading 状态重复实现
导师端与学生端相似展示重复实现但略有差异
如果存在，请明确哪些应该抽成共用组件 / 共用 hook / 共用 utils。
7）后端是否也需要做结构治理
请你不要只看前端。
后端也请检查：
Go API
experiment service / repository / handler / dto 是否边界清晰
传承链逻辑是否集中
删除权限 / confirm 逻辑 / head 语义是否收口良好
DTO 是否越来越臃肿
Express API
reports routes 是否开始变重
links / selected_dates / AI generation / comments / review 是否耦合在一个大文件里
service 层是否需要进一步拆分
与 AI 相关调用是否已足够模块化
如果后端存在“先能跑，后续会越来越乱”的风险，请一起提出重构建议。
四、我希望你先输出的内容
请先不要直接大改代码，先给我一份结构治理方案，按下面格式输出：
你对当前代码结构问题的整体判断
前端最需要治理的模块 / 文件
后端最需要治理的模块 / 文件
哪些逻辑应该下沉到 hook / service / domain / util
哪些组件应该拆分，怎么拆
哪些类型 / API client 需要集中整理
哪些重复逻辑值得抽共用层
建议的重构顺序（按优先级）
这次建议先做的最小重构范围
风险点和注意事项
五、我对这次重构的期望
请按正式产品标准来做，不是 demo 式“顺手整理一下”。
重点目标是：
页面层更薄
业务逻辑更集中
组件职责更清晰
类型更统一
重复更少
导师端 / 学生端语义边界更清楚
后面继续加功能时，不会越来越难改
六、实施原则
等我确认你的治理方案后，你再开始动代码。
请注意：
不要为了重构而大面积破坏已有功能
每一轮重构都要可验证、可回归
优先做“高收益、低风险”的结构优化
不要把所有东西一次性全重写