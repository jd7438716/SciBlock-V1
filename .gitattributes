# Auto detect text files and perform LF normalization
* text=auto

# Force bash scripts to always use LF line endings
*.sh text eol=lf

# Force Dockerfile to use LF
Dockerfile* text eol=lf

# Force migration SQL files to use LF
*.sql text eol=lf
